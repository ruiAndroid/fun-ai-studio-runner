# fun-ai-studio-runner 真实配置文件（当前按内网环境可直接提交）
#
# 说明：
# - systemd 推荐用 EnvironmentFile 加载本文件，例如：
#   EnvironmentFile=/opt/fun-ai-studio/config/runner.env
# - 如果你直接在仓库内维护本文件，部署时可将其复制到 /opt/fun-ai-studio/config/runner.env
#

# Deploy 控制面（100 内网）
DEPLOY_BASE_URL=http://172.21.138.100:7002

# Runner 标识（每台 Runner 唯一）
RUNNER_ID=runner-101-01

# 领取任务租约（秒）与轮询间隔（秒）
JOB_LEASE_SECONDS=30
POLL_SECONDS=3

# Runner -> Runtime-Agent 鉴权 Token（Header: X-Runtime-Token）
# TODO：请替换为你 runtime-agent 实际配置的 token（保持一致）
RUNTIME_AGENT_TOKEN=8f3d1a6c9e0b4f2d7c5a1e9b6d3f0c8a2e7b4d1f9c6a3b0e5d8c2f7a1b9e4d0c


