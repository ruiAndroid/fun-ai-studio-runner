# fun-ai-studio-runner 配置模板（不要提交任何真实密码）
# 建议复制到：/opt/fun-ai-studio/config/runner.env
#
# systemd 示例：
#   EnvironmentFile=/opt/fun-ai-studio/config/runner.env
#

# Deploy 控制面（内网）
DEPLOY_BASE_URL=http://172.21.138.100:7002

# Runner 标识（每台 Runner 唯一）
RUNNER_ID=runner-101-01

# 领取任务租约（秒）与轮询间隔（秒）
JOB_LEASE_SECONDS=30
POLL_SECONDS=3

# Runner -> Runtime-Agent 鉴权 Token（Header: X-Runtime-Token）
RUNTIME_AGENT_TOKEN=CHANGE_ME

# 日志目录
FUNAI_LOG_DIR=/data/funai/logs/fun-ai-studio/fun-ai-studio-runner

# Git 拉取（Runner bot）
GIT_SSH_KEY_PATH=/opt/fun-ai-studio/keys/gitea/runner-bot
GIT_KNOWN_HOSTS_PATH=/opt/fun-ai-studio/keys/gitea/known_hosts

# ACR（必须：否则无法 pull/push 私有镜像）
# 说明：这里历史变量名叫 ACR_*，但也可用于“自建镜像站”的 registry 地址。
ACR_REGISTRY=172.21.138.103:5000
ACR_NAMESPACE=funaistudio
# 推荐使用 REGISTRY_*（Runner 会 fallback 到 ACR_*，便于兼容）
REGISTRY_USERNAME=registry_user
REGISTRY_PASSWORD=CHANGE_ME_STRONG_PASSWORD


